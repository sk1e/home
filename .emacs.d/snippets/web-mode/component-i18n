# -*- mode: snippet -*-
# name: sscomp-i18n
# key: sscomp-i18n
# --
import * as React from 'react';
import block from 'bem-cn';

import './`(buffer-file-name/no-extension)`.scss';

import { ITranslateProps, i18nConnect } from 'services/i18n';

interface IOwnProps {
}

type IProps = IOwnProps & ITranslateProps;

const b = block('`(camel-case->lisp-case (buffer-file-name/no-extension))`');

class `(buffer-file-name/no-extension)` extends React.PureComponent<IProps> {
  public render() {
    return (
      <div className={b()}>
        $0
      </div>
    );
  }
}

export default i18nConnect(`(buffer-file-name/no-extension)`);
