# -*- mode: snippet -*-
# name: sscomp
# key: sscomp
# --
import * as React from 'react';
import block from 'bem-cn';

import './style.scss';

type Props = {
}

const b = block('`(camel-case->lisp-case (containing-directory))`');

class `(containing-directory)` extends React.PureComponent<Props> {
  public render() {
    return (
      <div className={b()}>
        $0
      </div>
    );
  }
}

export { `(containing-directory)` };
